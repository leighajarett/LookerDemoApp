import { Heading, Box } from '@looker/components'
import React from "react"


export default function Tickets(){
    return(
        <Box>
            <Heading variant="secondary">Submit Requests to the Demo Team</Heading>
        </Box>
    )}